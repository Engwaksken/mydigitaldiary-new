<?php

/*
|--------------------------------------------------------------------------
| Middleware Aliases (APPLIED)
|--------------------------------------------------------------------------
|
| This is a documentation snippet — not loaded by Laravel.
|
| The 'subscribed', 'admin', 'support.staff', and 'mobile.idempotent'
| aliases are already registered in bootstrap/app.php inside the
| withMiddleware() closure via $middleware->alias([...]).
*/
